<div id="pager">

	<% if(page.count > 1) { %>
	
		<% if(page.number) { %>
			<a href="/?page=<%= page.number %>" id="prev">前へ</a>
		<% } %>

		<% if(page.number < page.count - 1) { %>
			<a href="/?page=<%= page.number + 2 %>" id="next">次へ</a>
		<% } %>

	<% } %>

</div>